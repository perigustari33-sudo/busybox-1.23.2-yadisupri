name: Build BusyBox Static for MDM9607 with Android NDK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Android NDK
      run: |
        # Download Android NDK r26b
        wget -c https://dl.google.com/android/repository/android-ndk-r26b-linux.zip
        unzip -q android-ndk-r26b-linux.zip
        
        # Set environment variables
        export NDK_PATH=$(pwd)/android-ndk-r26b
        export PATH=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64/bin:$PATH
        echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV
        echo "PATH=$PATH" >> $GITHUB_ENV
        
        # Verifikasi compiler
        armv7a-linux-androideabi21-clang --version

    - name: Create minimal config for MDM9607
      run: |
        # Buat konfigurasi minimal yang sudah teruji
        cat > .config << 'EOF'
        CONFIG_HAVE_DOT_CONFIG=y

        #
        # Busybox Settings
        #
        CONFIG_DESKTOP=y
        CONFIG_EXTRA_COMPAT=y
        CONFIG_INCLUDE_SUSv2=y
        CONFIG_USE_PORTABLE_CODE=y
        CONFIG_PLATFORM_LINUX=y
        CONFIG_FEATURE_BUFFERS_USE_MALLOC=y
        CONFIG_SHOW_USAGE=y
        CONFIG_FEATURE_VERBOSE_USAGE=y
        CONFIG_FEATURE_COMPRESS_USAGE=y
        CONFIG_LONG_OPTS=y
        CONFIG_FEATURE_DEVPTS=y
        CONFIG_FEATURE_CLEAN_UP=y
        CONFIG_FEATURE_PIDFILE=y
        CONFIG_FEATURE_SUID=y
        CONFIG_FEATURE_PREFER_APPLETS=y
        CONFIG_BUSYBOX_EXEC_PATH="/proc/self/exe"
        CONFIG_FEATURE_SYSLOG=y

        #
        # Build Options
        #
        CONFIG_STATIC=y
        CONFIG_LFS=y
        CONFIG_FEATURE_LFS=y
        CONFIG_CROSS_COMPILER_PREFIX="armv7a-linux-androideabi21-"
        CONFIG_EXTRA_CFLAGS="-D_LARGEFILE_SOURCE -D_LARGEFILE64_SOURCE -D_FILE_OFFSET_BITS=64"

        #
        # Coreutils (minimal)
        #
        CONFIG_CAT=y
        CONFIG_ECHO=y
        CONFIG_FEATURE_FANCY_ECHO=y
        CONFIG_LS=y
        CONFIG_FEATURE_LS_FILETYPES=y
        CONFIG_FEATURE_LS_RECURSIVE=y
        CONFIG_MKDIR=y
        CONFIG_PWD=y
        CONFIG_RM=y
        CONFIG_TRUE=y
        CONFIG_FALSE=y
        CONFIG_TEST=y
        CONFIG_TOUCH=y
        CONFIG_UNIQ=y
        CONFIG_WHOAMI=y
        CONFIG_BASENAME=y
        CONFIG_DIRNAME=y
        CONFIG_HEAD=y
        CONFIG_TAIL=y
        CONFIG_SLEEP=y
        CONFIG_SYNC=y

        #
        # Shells
        #
        CONFIG_ASH=y
        CONFIG_ASH_BASH_COMPAT=y
        CONFIG_ASH_JOB_CONTROL=y
        CONFIG_ASH_ALIAS=y
        CONFIG_ASH_GETOPTS=y
        CONFIG_ASH_BUILTIN_ECHO=y
        CONFIG_ASH_BUILTIN_PRINTF=y
        CONFIG_ASH_BUILTIN_TEST=y
        CONFIG_ASH_CMDCMD=y
        CONFIG_ASH_OPTIMIZE_FOR_SIZE=y

        #
        # Linux System Utilities
        #
        CONFIG_DMESG=y
        CONFIG_MORE=y
        CONFIG_MOUNT=y
        CONFIG_UMOUNT=y

        #
        # Process Utilities
        #
        CONFIG_FREE=y
        CONFIG_KILL=y
        CONFIG_KILLALL=y
        CONFIG_PS=y
        CONFIG_FEATURE_PS_WIDE=y
        CONFIG_TOP=y
        CONFIG_UPTIME=y

        #
        # Networking Utilities
        #
        CONFIG_PING=y
        CONFIG_WGET=y
        CONFIG_FEATURE_WGET_STATUSBAR=y

        #
        # Editors
        #
        CONFIG_VI=y
        CONFIG_FEATURE_VI_MAX_LEN=1024
        CONFIG_FEATURE_VI_COLON=y
        CONFIG_FEATURE_VI_YANKMARK=y
        CONFIG_FEATURE_VI_SEARCH=y
        CONFIG_FEATURE_VI_USE_SIGNALS=y
        CONFIG_FEATURE_VI_DOT_CMD=y

        #
        # Init Utilities
        #
        CONFIG_INIT=y
        CONFIG_FEATURE_USE_INITTAB=y
        CONFIG_FEATURE_INIT_SCTTY=y
        CONFIG_FEATURE_INIT_SYSLOG=y
        CONFIG_FEATURE_INIT_COREDUMPS=y
        CONFIG_HALT=y

        #
        # Archival Utilities
        #
        CONFIG_GUNZIP=y
        CONFIG_GZIP=y
        CONFIG_TAR=y
        CONFIG_FEATURE_TAR_CREATE=y
        CONFIG_UNZIP=y

        #
        # Login/Password
        #
        CONFIG_GETTY=y
        CONFIG_LOGIN=y
        CONFIG_PASSWD=y
        EOF

    - name: Build BusyBox with verbose output
      run: |
        export CC=armv7a-linux-androideabi21-clang
        export CXX=armv7a-linux-androideabi21-clang++
        export AR=llvm-ar
        export AS=llvm-as
        export LD=ld.lld
        export STRIP=llvm-strip
        export CROSS_COMPILE=armv7a-linux-androideabi21-
        export ARCH=arm
        
        # Build dengan verbose untuk melihat error detail
        make V=1 -j$(nproc) \
          CC="$CC" \
          CXX="$CXX" \
          AR="$AR" \
          AS="$AS" \
          LD="$LD" \
          STRIP="$STRIP" \
          CFLAGS="-Os -pipe -static -march=armv7-a -mfpu=vfpv4-d16 -mfloat-abi=softfp -D_LARGEFILE_SOURCE -D_LARGEFILE64_SOURCE -D_FILE_OFFSET_BITS=64" \
          LDFLAGS="-static" \
          SKIP_STRIP=n 2>&1 | tee build.log
        
        # Cek hasil
        file busybox || true
        ls -lh busybox || true

    - name: Check build log for errors
      if: failure()
      run: |
        echo "=== Build Log (last 100 lines) ==="
        tail -100 build.log
        echo ""
        echo "=== Errors in build log ==="
        grep -i "error:" build.log || echo "No errors found"

    - name: Test binary with QEMU
      if: success()
      run: |
        sudo apt-get update
        sudo apt-get install -y qemu-user-static
        qemu-arm-static ./busybox --version
        qemu-arm-static ./busybox echo "Test from Android NDK build"

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: busybox-mdm9607-ndk
        path: |
          busybox
          busybox_unstripped
          .config
          build.log
        retention-days: 30
