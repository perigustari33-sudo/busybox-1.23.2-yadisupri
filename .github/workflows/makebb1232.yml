name: Build BusyBox with GCC 4.9

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-18.04  # Kembali ke Ubuntu 18.04 yang masih support GCC 4.9

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install GCC 4.9
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          gcc-4.9-arm-linux-gnueabihf \
          binutils-arm-linux-gnueabihf \
          libc6-dev-armhf-cross \
          make \
          bc \
          bison \
          flex
        
        arm-linux-gnueabihf-gcc-4.9 --version

    - name: Build BusyBox
      run: |
        export CROSS_COMPILE=arm-linux-gnueabihf-
        export ARCH=arm
        
        # Use your config
        cp configs/k02a.config .config
        echo "CONFIG_STATIC=y" >> .config
        
        make oldconfig
        make -j$(nproc) CC=arm-linux-gnueabihf-gcc-4.9
        
        file busybox

    - name: Upload artifacts
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: busybox-gcc49
        path: busybox
        retention-days: 30
